# Core
numpy>=1.24,<2.1
pandas>=2.0
scikit-learn>=1.3

# Deep learning. Keras 3 (bundled with TensorFlow >= 2.16) is what this code
# targets; it also runs on the tf.keras of 2.13-2.15. No upper pin, because
# TF 2.13-2.16 have no wheels for Python 3.13+.
#
# TensorFlow 2.11+ has no native GPU support on Windows -- use WSL2,
# tensorflow-directml-plugin, or Google Colab for GPU acceleration.
tensorflow>=2.13

# Class balancing (optional; the pipeline degrades gracefully without it)
imbalanced-learn>=0.11

# Figures
matplotlib>=3.7
seaborn>=0.12
